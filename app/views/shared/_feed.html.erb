<% if @feed_items.any? %>
  <ol class="microposts">
    <%= render @feed_items %>
  </ol>
  <%= will_paginate @feed_items,
  # ---without beow we are not able to go to the 2nd page of posts - Rails trying to go to nonexistent Micropost index page---
                    params: { controller: :static_pages, action: :home } %>
<% end %>
